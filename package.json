{
  "name": "vuepress-homepage",
  "version": "0.0.1",
  "description": "Generate Yofer's Home with VuePress",
  "author": "Fing <1732562137@qq.com>",
  "repository": "https://github.com/YoferChen/YoferVuePressHome.git",
  "scripts": {
    "clean": "rimraf dist/*",
    "dev": "set NODE_OPTIONS=--openssl-legacy-provider && vuepress dev docs --port=3000",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && vuepress build docs"
  },
  "license": "MIT",
  "devDependencies": {
    "vuepress": "^1.9.10"
  },
  "dependencies": {
    "markdown-it": "^8.4.1",
    "markdown-it-emoji": "^1.4.0",
    "markdown-it-katex": "^2.0.3"
  }
}
